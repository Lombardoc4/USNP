{
  "name": "propeller-onboarding-api",
  "version": "1.0.0",
  "description": "Express API for new employees to save progress",
  "main": "index.js",
  "scripts": {
    "db": "mongod --config /usr/local/etc/mongod.conf --fork",
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cris Lombardo",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
